/** Editorial mono eyebrow label used across the marketing site. */
export const Eyebrow = ({ children }: { children: React.ReactNode }) => (
  <span className="font-mono text-[11px] font-medium uppercase tracking-[0.18em] text-brand-600 dark:text-brand-400">
    {children}
  </span>
);
